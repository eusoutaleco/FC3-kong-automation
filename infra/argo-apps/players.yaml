apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: players
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    path: deployments/players
    repoURL: https://github.com/eusoutaleco/fc-championships-app-players-app
    targetRevision: HEAD
    helm:
      valueFiles:
        - values-kong.yaml
  destination:
    namespace: bets
    server: http://127.0.0.1:57414
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
